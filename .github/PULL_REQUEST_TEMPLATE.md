## What this changes

<!-- What the change does, and why. The diff already shows the "what" in detail, so lead with the
     motivation: the constraint, bug, or gap that made this necessary. -->

## How it was verified

<!-- Commands you ran and what they reported, e.g. `dotnet test <solution>` -> N passed.
     Say so plainly if something is untested or was only checked manually. -->

## Checklist

- [ ] `dotnet build` is clean (warnings are errors in this repo)
- [ ] `dotnet test` passes
- [ ] Public API changes are documented with XML comments
- [ ] Commit messages follow [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/)
